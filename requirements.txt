# Core dependencies
boto3>=1.26.0
botocore>=1.29.0

# Strands integration (v2.0.0)
strands-agents>=0.1.0  # AWS Strands Agents framework

# Optional: For example agents (deprecated in v2.0)
anthropic>=0.18.0  # Claude API for standalone examples (use Strands instead)

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
mypy>=1.0.0
ruff>=0.1.0
