# Core dependencies
boto3>=1.26.0
botocore>=1.29.0

# Agent runtime (for Strands integration)
anthropic>=0.18.0  # Claude API for agent runtime

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
mypy>=1.0.0
ruff>=0.1.0
